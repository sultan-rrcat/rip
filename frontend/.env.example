# Backend API base URL used by `npm run dev`.
# Copy this file to `.env` (gitignored) and adjust to your machine.
# Empty = same-origin relative URLs (prod nginx proxies /api/ + /v1/).
VITE_API_URL=http://localhost:8000
